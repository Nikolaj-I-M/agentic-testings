import type { Level } from "./types.js";

export const placeholderLevel: Level = Object.freeze({
  width: 800,
  height: 450,
  entity: Object.freeze({
    x: 360,
    y: 190,
    width: 80,
    height: 80,
    color: "#f05a9d",
  }),
});
